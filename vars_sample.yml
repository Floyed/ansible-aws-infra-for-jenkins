---
vpc_name: jenkins_vpc
aws_region: us-east-1
profile: default
cidr_block: 10.0.0.0/16
vpc_subnets: 
  private-1:
    cidr: 10.0.1.0/24
    az: "us-east-1b"

# keyname from aws
ssh_key_name: key_xxx

# AMI name which contains docker, nginx and certbot
image: ami-xxx

#
ansible_ssh_private_key_file: "/home/user/xyz.pem"